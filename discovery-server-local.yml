eureka:
  instance:
    hostname: "localhost"
  client:
    serviceUrl:
      defaultZone: "http://localhost:8888/eureka/"
    register-with-eureka: false
    fetch-registry: false

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always